[project]
name = "rt-sigma"
version = "0.1.0"
authors = [
    { name = "Alain Muls", email = "alain.muls@satnavam.be" },
]
description = "A Python package for real-time detection of GNSS interferences."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
]
